PRINT_DECIMAL PROC
	MOV AX,DX
	MOV DX,0
	MOV CX,0
	MOV BX,10

	AGAIN1:
		DIV BX
		PUSH DX
		INC CX
		MOV DX,0
		CMP AX,0
		JNE AGAIN1
	
	AGAIN2:
		POP DX
		ADD DX,30H
		MOV AH,02
		INT 21H
		DEC CX
		JNZ AGAIN2

	RET
PRINT_DECIMAL ENDP
